//Steps to adding firebase to android studio flutter project
/**
 * 1. Creating a firebase project
 * 2. Downloading some files and adding them to the android studion flutter project
 * 3. Installing the SDKS
 * 4.Initializing the firebase platform in your code
 */
/*
import 'package:firebase_core/firebase_core.dart';
class User {
  final String id;
  final String profileName;
  final String username;
  final String url;
  final String email;
  final String bio;

  User({this.id, this.profileName, this.username, this.url, required this.email, this.bio});

  factory User.fromDocument(DocumentSnapshot doc){
    return User(id: doc.documentID,
        profileName:doc['displayName'],
        username: doc['username'],
        url:doc['photoUrl'],
        email: doc['email'],
        bio:doc['bio']);
  }

}*/