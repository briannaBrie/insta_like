import 'package:flutter/material.dart';
import 'package:flutter/cupertino.dart';

const TextStyle kBodyText = TextStyle(fontSize: 22, color: Colors.white, height: 1.5);
const TextStyle dropText = TextStyle(fontSize: 18, color: Colors.white, height: 1.5);
const TextStyle headerText = TextStyle(fontSize: 32,color:Colors.pink,height: 1.5);
const Color kWhite = Colors.white;
const Color kBlue = Color(0xff5663ff);
const Color kDark = Color(0xff292728);
const footerColor = Color(0xFF131313);

const Color backgroundColor = Color(0xFF191D1F);

/// Slightly lighter version of [backgroundColor].
const Color backgroundFadedColor = Color(0xFF191B1C);

/// Color used for cards and surfaces.
const Color cardColor = Color(0xFF1F2426);

/// Accent color used in the application.
const Color accentColor = Colors.pink;
const Color kOrange = Colors.deepOrangeAccent;
const Color kPurple = Colors.purple;
const Color kAmber = Colors.amber;
const Color kLightBlue = Colors.lightBlueAccent;

